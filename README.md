# Jewelry Helper - Аналитическая платформа для ювелирных товаров

Аналитическая платформа для сбора, очистки, предобработки, разметки и анализа данных о ювелирных изделиях с сайта 585zolotoy.ru.

## 📋 Описание проекта

Проект представляет собой комплексную аналитическую платформу, которая помогает улучшить бизнес-процессы в области продажи ювелирных изделий за счет:

- **Сбора данных** - автоматизированный парсинг товаров с сайта
- **Очистки данных** - нормализация и валидация собранных данных
- **Предобработки** - подготовка данных для анализа
- **Хранения данных** - использование PostgreSQL и MinIO
- **Анализа данных** - выявление паттернов и инсайтов
- **Визуализации** - представление результатов в графическом виде
- **Оценки качества** - автоматизированные проверки качества данных
- **Генерации отчетов** - создание подробных отчетов с рекомендациями

## 🏗️ Структура проекта

```
jewelry-helper/
├── config/                  # Конфигурационные файлы
│   ├── __init__.py
│   └── settings.py          # Настройки (URLs, БД, MinIO, паузы)
│
├── src/                     # Основные модули парсера
│   ├── __init__.py
│   ├── parser.py            # Парсинг страниц товаров и сбор ссылок
│   ├── extractors.py        # Извлечение данных из HTML
│   ├── storage.py           # Работа с БД и MinIO
│   └── selenium_utils.py    # Настройка Selenium драйвера
│
├── utils/                   # Утилиты
│   ├── __init__.py
│   ├── helpers.py           # Вспомогательные функции
│   └── validators.py        # Валидация данных продуктов
│
├── cleaners/                # Очистка данных
│   ├── __init__.py
│   └── data_cleaner.py      # Функции очистки и нормализации
│
├── preprocessing/           # Предобработка данных
│   ├── __init__.py
│   └── data_preprocessor.py # Нормализация, стандартизация, кодирование
│
├── quality/                 # Оценка качества данных
│   ├── __init__.py
│   └── data_quality.py      # Метрики и проверки качества данных
│
├── analytics/               # Анализ данных
│   ├── __init__.py
│   └── data_analyzer.py     # Анализ цен, категорий, характеристик
│
├── visualization/           # Визуализация данных
│   ├── __init__.py
│   └── data_visualizer.py   # Построение графиков и диаграмм
│
├── reports/                 # Генерация отчетов
│   ├── __init__.py
│   └── report_generator.py  # Создание отчетов и сохранение результатов
│
├── main.py                  # Точка входа для парсинга
├── run_analysis.py          # Скрипт для запуска анализа
├── requirements.txt         # Зависимости проекта
└── README.md               # Документация
```

## 🚀 Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd jewelry-helper
```

2. **Создайте виртуальное окружение (если еще не создано):**
```bash
python -m venv venv
```

3. **Активируйте виртуальное окружение:**
```bash
# Windows (PowerShell)
.\venv\Scripts\Activate.ps1

# Windows (CMD)
venv\Scripts\activate.bat

# Linux/Mac
source venv/bin/activate
```

После активации в начале строки терминала должно появиться `(venv)`.

4. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

**Важно:** Убедитесь, что виртуальное окружение активировано перед запуском скриптов!

4. **Настройте базу данных PostgreSQL:**
   - Создайте базу данных `jewelry`
   - Создайте пользователя `jewelry_user`
   - Настройте параметры в `config/settings.py`

5. **Настройте MinIO:**
   - Запустите MinIO сервер
   - Настройте параметры в `config/settings.py`

## 📊 Использование

### Сбор данных (парсинг)

Запуск парсера для сбора данных о товарах:

```bash
python main.py
```

Парсер:
- Собирает ссылки на товары из каталогов
- Парсит информацию о каждом товаре
- Валидирует и очищает данные
- Сохраняет данные в PostgreSQL
- Сохраняет изображения в MinIO

### Анализ данных

Запуск анализа собранных данных:

```bash
python run_analysis.py
```

Скрипт выполняет:
- Оценку качества данных
- Анализ цен, категорий, характеристик
- Генерацию визуализаций
- Создание отчетов

Результаты сохраняются в директории `reports/`.

## 🔧 Модули проекта

### 1. Сбор данных (`src/`)

- **parser.py** - основная логика парсинга страниц
- **extractors.py** - извлечение данных из HTML
- **storage.py** - сохранение в БД и MinIO
- **selenium_utils.py** - настройка веб-драйвера

### 2. Очистка данных (`cleaners/`, `utils/validators.py`)

- **data_cleaner.py** - нормализация текста, очистка характеристик
- **validators.py** - валидация URL, цен, названий, описаний

### 3. Предобработка данных (`preprocessing/`)

- Нормализация данных
- Стандартизация числовых значений
- Обработка пропущенных значений
- Обработка выбросов
- Кодирование категориальных данных
- Извлечение признаков из характеристик

### 4. Оценка качества данных (`quality/`)

Метрики качества:
- **Completeness** - полнота данных (отсутствие пропусков)
- **Consistency** - согласованность (проверка дубликатов, типов)
- **Accuracy** - точность (валидность цен, URL, названий)
- **Validity** - валидность (соответствие форматам)

Автоматизированные проверки качества данных.

### 5. Анализ данных (`analytics/`)

Анализ включает:
- Статистику по ценам
- Распределение по категориям
- Анализ характеристик товаров
- Генерацию инсайтов и рекомендаций

### 6. Визуализация (`visualization/`)

Графики:
- Распределение цен (гистограмма, box plot)
- Распределение по категориям
- Цены по категориям
- Heatmap характеристик

### 7. Генерация отчетов (`reports/`)

Типы отчетов:
- **Аналитический отчет** - детальный анализ данных
- **Отчет о качестве** - оценка качества данных
- **Сводный отчет** - краткая сводка по основным метрикам
- **Визуализации** - графики и диаграммы

## 📈 Бизнес-инсайты

Платформа позволяет получить:

1. **Анализ ценового сегмента** - распределение товаров по ценам
2. **Популярные категории** - какие категории товаров наиболее представлены
3. **Характеристики товаров** - анализ ключевых характеристик
4. **Оценка качества данных** - метрики полноты и валидности данных
5. **Рекомендации** - на основе анализа данных

## 🔒 Конфигурация

Настройки находятся в `config/settings.py`:

- **BASE_URL** - базовый URL сайта
- **CATALOG_URLS** - список URL каталогов для парсинга
- **DB_CONFIG** - параметры подключения к PostgreSQL
- **MINIO_CONFIG** - параметры подключения к MinIO
- **PAUSE_CARD**, **PAUSE_CATALOG** - паузы между запросами

## 📝 Требования

- Python 3.9+
- PostgreSQL 12+
- MinIO (для хранения изображений)
- Chrome/Chromium (для Selenium)

## 🛠️ Технологии

- **Парсинг**: Selenium, BeautifulSoup
- **База данных**: PostgreSQL, psycopg2
- **Хранилище**: MinIO
- **Анализ**: Pandas, NumPy
- **Визуализация**: Matplotlib, Seaborn
- **ML/Preprocessing**: scikit-learn
